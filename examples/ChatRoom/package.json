{
  "name": "jellydoodle-example-chatroom",
  "version": "1.0.0",
  "description": "",
  "main": "client.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.min.js",
    "prestart": "babel client.js --out-dir http/js/ --no-comments --minify && babel server.js --out-dir ./ --out-file server.min.js --no-comments --minify"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-plugin-transform-es2015-modules-commonjs": "^6.26.0",
    "babel-preset-env": "^1.6.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-latest": "^6.24.1",
    "colors": "^1.1.2"
  },
  "dependencies": {
    "babel-preset-es2015": "^6.24.1",
    "express": "^4.16.1",
    "socketio": "^1.0.0"
  }
}
